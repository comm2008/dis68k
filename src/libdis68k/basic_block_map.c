// Basic Block Map //

/*typedef struct bb {
    bb_map *source_map;
    off_t offset;
    size_t encoding_size;
    size_t count;
    i68k *body;
} bb;

typedef struct bb_map {
    void *buffer;
    size_t buffer_size;
    rbtreen
} bb_map;

int bb_map_init_buffer(void *buf, size_t size);
int bb_map_init_file(int fd);
int bb_map_insert(bb_map m, bb );
int bb_map_serialize();*/
